// @flow
import * as React from 'react';

type Props = {};

export function ChildYear(props: Props) {
    return (
        <main className={'childYear'}>
            <h2>ChildYear</h2>

        </main>
    );
};