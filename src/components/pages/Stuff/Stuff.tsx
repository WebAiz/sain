// @flow
import * as React from 'react';

type Props = {
    
};

export function Stuff(props: Props) {
    return (
        <main className={'stuff'}>
            <h2>Stuff</h2>
        </main>
    );
};